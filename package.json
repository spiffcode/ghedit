{
  "name": "ghcode",
  "description": "GH Code",
  "version": "0.0.1",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-shell": "^0.5.2"
  },
  "scripts": {
    "clean": "gulp clean",
    "build": "gulp build",
    "watch": "gulp watch",
    "push": "gulp push"
  }
}
