import 'vs/css!./media/extensionsViewlet';
import { TPromise } from 'vs/base/common/winjs.base';
import { Builder, Dimension } from 'vs/base/browser/builder';
import Event from 'vs/base/common/event';
import { IAction } from 'vs/base/common/actions';
import { Viewlet } from 'vs/workbench/browser/viewlet';
import { ITelemetryService } from 'vs/platform/telemetry/common/telemetry';
import { IInstantiationService } from 'vs/platform/instantiation/common/instantiation';
import { IExtensionsWorkbenchService, IExtensionsViewlet } from './extensions';
import { IExtensionManagementService, IExtensionGalleryService } from 'vs/platform/extensionManagement/common/extensionManagement';
import { IProgressService } from 'vs/platform/progress/common/progress';
import { IWorkbenchEditorService } from 'vs/workbench/services/editor/common/editorService';
export declare class ExtensionsViewlet extends Viewlet implements IExtensionsViewlet {
    private galleryService;
    private extensionService;
    private progressService;
    private instantiationService;
    private editorService;
    private extensionsWorkbenchService;
    private _onSearchChange;
    onSearchChange: Event<string>;
    private searchDelayer;
    private root;
    private searchBox;
    private extensionsBox;
    private list;
    private primaryActions;
    private secondaryActions;
    private disposables;
    constructor(telemetryService: ITelemetryService, galleryService: IExtensionGalleryService, extensionService: IExtensionManagementService, progressService: IProgressService, instantiationService: IInstantiationService, editorService: IWorkbenchEditorService, extensionsWorkbenchService: IExtensionsWorkbenchService);
    create(parent: Builder): TPromise<void>;
    setVisible(visible: boolean): TPromise<void>;
    focus(): void;
    layout({height, width}: Dimension): void;
    getOptimalWidth(): number;
    getActions(): IAction[];
    getSecondaryActions(): IAction[];
    search(text: string, immediate?: boolean): void;
    private triggerSearch(immediate?, suggestPopular?);
    private doSearch(text?, suggestPopular?);
    private onExtensionSelected(extension);
    private onEnter();
    private onEscape();
    private onUpArrow();
    private onDownArrow();
    private onPageUpArrow();
    private onPageDownArrow();
    dispose(): void;
}
